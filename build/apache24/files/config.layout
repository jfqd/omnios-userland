<Layout SolAmd64>
    prefix:        /usr/local/apache24
    exec_prefix:   ${prefix}
    bindir:        ${exec_prefix}/bin/amd64
    sbindir:       ${exec_prefix}/bin/amd64
    libdir:        ${exec_prefix}/lib
    libexecdir:    ${exec_prefix}/libexec/amd64
    mandir:        ${prefix}/man
    sysconfdir:    ${prefix}/conf
    datadir:       ${prefix}/share
    installbuilddir: ${datadir}/build/amd64
    errordir:      ${datadir}/error
    iconsdir:      ${datadir}/icons
    htdocsdir:     ${datadir}/htdocs
    manualdir:     ${datadir}/manual
    cgidir:        ${datadir}/cgi-bin
    includedir:    ${prefix}/include/amd64
    localstatedir: ${prefix}/var
    runtimedir:    ${localstatedir}/run
    logfiledir:    ${localstatedir}/log
    proxycachedir: ${localstatedir}/proxy
</Layout>

<Layout Soli386>
    prefix:        /usr/local/apache24
    exec_prefix:   ${prefix}
    bindir:        ${exec_prefix}/bin/i386
    sbindir:       ${exec_prefix}/bin/i386
    libdir:        ${exec_prefix}/lib
    libexecdir:    ${exec_prefix}/libexec/i386
    mandir:        ${prefix}/man
    sysconfdir:    ${prefix}/conf
    datadir:       ${prefix}/share
    installbuilddir: ${datadir}/build/i386
    errordir:      ${datadir}/error
    iconsdir:      ${datadir}/icons
    htdocsdir:     ${datadir}/htdocs
    manualdir:     ${datadir}/manual
    cgidir:        ${datadir}/cgi-bin
    includedir:    ${prefix}/include/i386
    localstatedir: ${prefix}/var
    runtimedir:    ${localstatedir}/run
    logfiledir:    ${localstatedir}/log
    proxycachedir: ${localstatedir}/proxy
</Layout>
